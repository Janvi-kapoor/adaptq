/* storage/contiguous_slab.cpp — TU stub.
 * Class definition is in contiguous_slab.h (all methods inline).
 * This file exists so CMake can compile it into a library/test target
 * without requiring the header to be a true standalone compilation unit. */
#include "contiguous_slab.h"
